apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "travel-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "travel-service.selectorLabels" . | nindent 4 }}
