apiVersion: v1
kind: Service
metadata:
  name: travel-service
  namespace: production
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: travel-service
  ports:
    - port: 8000
      targetPort: 8000
