apiVersion: v1
kind: Service
metadata:
  name: utility-service
  namespace: production
  labels:
    app.kubernetes.io/name: utility-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: utility-service
