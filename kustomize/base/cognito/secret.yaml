# Placeholder — replace values after Terraform apply or use External Secrets Operator.
# terraform output -raw cognito_user_pool_id | base64
apiVersion: v1
kind: Secret
metadata:
  name: cognito-secret
  namespace: production
type: Opaque
stringData:
  COGNITO_USER_POOL_ID: "REPLACE_AFTER_TERRAFORM"
  COGNITO_CLIENT_ID: "REPLACE_AFTER_TERRAFORM"
  COGNITO_CLIENT_SECRET: "REPLACE_AFTER_TERRAFORM"
  COGNITO_DOMAIN: "planmyjourney.auth.us-east-1.amazoncognito.com"
