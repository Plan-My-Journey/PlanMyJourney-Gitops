apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 00-namespace.yaml
  - 01-gatewayclass.yaml
  - 01-envoyproxy-nlb.yaml
  - 02-gateway.yaml
  - 04-envoy-pdb.yaml
  # Requires cert-manager + ClusterIssuer letsencrypt-prod (install separately).
  # - 03-certificate.yaml
  - routes/prod-routes.yaml
  - routes/dev-routes.yaml
